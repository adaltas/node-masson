#!/usr/bin/env node

require('coffeescript/register');
const masson = require('../lib');

(async () => {
  try {
    await masson(process.argv.slice(2))
  } catch (err) {
    process.stderr.write(err.stack+'\n\n')
  }
})()
