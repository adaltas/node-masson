
# should = require 'should'
# plan = require '../src/plan'

# describe 'load', ->

#   it 'load dependencies', (next) ->
#     plan ['wand/test/resources/plan/a', 'wand/test/resources/plan/b'], (actions) ->
#       console.log actions
#       next()
